The following database has been written so that it can be deliberately compromised
by SQL Injection eg ' OR 1=1;

The code uses MySQL Workbench to create the db and the table, tbluser (single table)
to output data. 
